name: Quality checks

on:
  push:
    branches:
      - main
      - dev
      - codex/**
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - name: Install locked dependencies
        run: npm ci
      - name: Run backend tests
        run: npm test

  android:
    name: Android lint, tests and verified production-connected APK
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env: &android-test-env
      MBOTE_API_BASE_URL: https://mbote-backend.onrender.com/v1
      VITE_SOCKET_URL: https://mbote-backend.onrender.com
      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
      VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
      GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
      GITHUB_CLIENT_ID: ${{ secrets.GITHUB_CLIENT_ID }}
      GRADLE_OPTS: -Dorg.gradle.jvmargs=-Xmx3g -Dkotlin.daemon.jvm.options=-Xmx2g
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.10.2
      - name: Reject invalid production endpoints
        shell: bash
        run: |
          set -euo pipefail
          test "$MBOTE_API_BASE_URL" = "https://mbote-backend.onrender.com/v1"
          test "$VITE_SOCKET_URL" = "https://mbote-backend.onrender.com"
          ! printf '%s\n%s\n' "$MBOTE_API_BASE_URL" "$VITE_SOCKET_URL" | grep -q 'example.invalid'
      - name: Build installable debug APK
        run: gradle --no-daemon --max-workers=2 --stacktrace clean assembleDebug
      - name: Run Android unit tests
        run: gradle --no-daemon --max-workers=2 --stacktrace testDebugUnitTest
      - name: Run Android lint
        run: gradle --no-daemon --max-workers=2 --stacktrace lintDebug
      - name: Locate Android SDK build tools
        id: android-tools
        shell: bash
        run: |
          set -euo pipefail
          BUILD_TOOLS="$(find "$ANDROID_HOME/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -n 1)"
          test -x "$BUILD_TOOLS/apksigner"
          test -x "$BUILD_TOOLS/aapt"
          echo "build_tools=$BUILD_TOOLS" >> "$GITHUB_OUTPUT"
      - name: Verify APK signature and package
        shell: bash
        run: |
          set -euo pipefail
          APK="app/build/outputs/apk/debug/app-debug.apk"
          TOOLS="${{ steps.android-tools.outputs.build_tools }}"
          test -s "$APK"
          "$TOOLS/apksigner" verify --verbose --print-certs "$APK"
          BADGING="$($TOOLS/aapt dump badging "$APK")"
          echo "$BADGING"
          echo "$BADGING" | grep -q "package: name='com.aistudio.mbote.krtwvx.debug'"
          echo "$BADGING" | grep -q "sdkVersion:'26'"
          echo "$BADGING" > app/build/outputs/apk/debug/mbote-apk-info.txt
          sha256sum "$APK" > app/build/outputs/apk/debug/mbote-apk-sha256.txt
      - name: Prepare test APK artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p app/build/test-apk
          cp app/build/outputs/apk/debug/app-debug.apk app/build/test-apk/MBote-test.apk
          cp app/build/outputs/apk/debug/mbote-apk-info.txt app/build/test-apk/
          cp app/build/outputs/apk/debug/mbote-apk-sha256.txt app/build/test-apk/
      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-test-reports
          path: |
            app/build/reports/
            app/build/test-results/
          if-no-files-found: ignore
      - name: Upload branch APK
        if: github.ref != 'refs/heads/main' && success()
        uses: actions/upload-artifact@v4
        with:
          name: mbote-production-test-apk
          path: |
            app/build/outputs/apk/debug/app-debug.apk
            app/build/outputs/apk/debug/mbote-apk-info.txt
            app/build/outputs/apk/debug/mbote-apk-sha256.txt
          if-no-files-found: error
          retention-days: 30
      - name: Publish verified installable test APK
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && success()
        uses: actions/upload-artifact@v4
        with:
          name: mbote-installable-test-apk
          path: app/build/test-apk/
          if-no-files-found: error
          retention-days: 30
